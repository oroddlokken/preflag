name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5.6.0
        with:
          go-version: '1.25.5'
          cache: false

      - name: Run tests
        run: go test -v ./...

      - name: Build binaries
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          COMMIT=$(git rev-parse --short HEAD)
          DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          LDFLAGS="-s -w -X main.version=$VERSION -X main.commit=$COMMIT -X main.date=$DATE"

          mkdir -p dist

          # Linux glibc builds (matching eza platform names)
          GOOS=linux GOARCH=amd64 go build -ldflags "$LDFLAGS" -o dist/preflag-x86_64-unknown-linux-gnu
          GOOS=linux GOARCH=arm64 go build -ldflags "$LDFLAGS" -o dist/preflag-aarch64-unknown-linux-gnu
          GOOS=linux GOARCH=arm GOARM=6 go build -ldflags "$LDFLAGS" -o dist/preflag-arm-unknown-linux-gnueabihf

          # Linux musl builds (static)
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -ldflags "$LDFLAGS" -o dist/preflag-x86_64-unknown-linux-musl
          CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -ldflags "$LDFLAGS" -o dist/preflag-aarch64-unknown-linux-musl

          # macOS
          GOOS=darwin GOARCH=amd64 go build -ldflags "$LDFLAGS" -o dist/preflag-x86_64-apple-darwin
          GOOS=darwin GOARCH=arm64 go build -ldflags "$LDFLAGS" -o dist/preflag-aarch64-apple-darwin

          # Windows
          GOOS=windows GOARCH=amd64 go build -ldflags "$LDFLAGS" -o dist/preflag-x86_64-pc-windows-gnu.exe

          # Create checksums
          cd dist && sha256sum * > checksums.txt

      - name: Create Release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          files: |
            dist/preflag-x86_64-unknown-linux-gnu
            dist/preflag-aarch64-unknown-linux-gnu
            dist/preflag-arm-unknown-linux-gnueabihf
            dist/preflag-x86_64-unknown-linux-musl
            dist/preflag-aarch64-unknown-linux-musl
            dist/preflag-x86_64-apple-darwin
            dist/preflag-aarch64-apple-darwin
            dist/preflag-x86_64-pc-windows-gnu.exe
            dist/checksums.txt
          generate_release_notes: true

      - name: Extract version and checksums
        id: version
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          VERSION_NUM=${VERSION#v}
          SHA256_X86=$(grep "preflag-x86_64-apple-darwin" dist/checksums.txt | cut -d' ' -f1)
          SHA256_ARM=$(grep "preflag-aarch64-apple-darwin" dist/checksums.txt | cut -d' ' -f1)

          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "version_number=$VERSION_NUM" >> $GITHUB_OUTPUT
          echo "sha256_x86=$SHA256_X86" >> $GITHUB_OUTPUT
          echo "sha256_arm=$SHA256_ARM" >> $GITHUB_OUTPUT
